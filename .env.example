# Copy to .env and fill in. .env is gitignored; never commit real values.

# Hugging Face: username or org that owns the repos, and a fine-grained WRITE token.
HF_USERNAME=
HF_TOKEN=

# MLflow tracking server. Leave empty to log to a local ./mlruns folder.
MLFLOW_TRACKING_URI=

# Optional
HF_HOME=
